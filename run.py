import random
import os
import sys

# Define the board size and number of mines
BOARD_SIZE = 10
NUM_MINES = 10

class Board:
    def __init__(self, size):
        self.size = size # Size of the game board
        self.board = [["." for _ in range (size)] for _ in range(size)] #Initialize the board with empty spaces
        self.mines = [] # List to store mines position
        self.start = (0, 0) # Start top-left corner
        self goal = (size -1, size -1) # Finish bottom-right corner


#Main functions 
def main():


if __name__ == "__main__"
    main()


